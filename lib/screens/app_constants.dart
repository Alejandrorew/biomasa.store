import 'package:flutter/material.dart';

// AJUSTE: Centralización de la paleta de colores para uso global.
const Color kNavyDark = Color(0xFF001534); // Fondo principal
const Color kCardBg = Color(0xFFF2F2F2);   // Fondo de tarjeta/campos
const Color kBlue = Color(0xFF426A8C);     // Íconos, acentos, enlaces
const Color kNavy = Color(0xFF233A59);     // Tarjeta, headers, botones

// AJUSTE: Definición de un sistema de espaciado consistente.
const double kSmallPadding = 8.0;
const double kMediumPadding = 16.0;
const double kLargePadding = 24.0;
const double kXLargePadding = 32.0;

// AJUSTE: Definición de valores de borde consistentes.
const double kDefaultBorderRadius = 12.0;

// AJUSTE: Definición de tamaños de fuente consistentes.
const double kPrimaryTextStyleSize = 18.0;
const double kSecondaryTextStyleSize = 14.0;
const double kLinkTextStyleSize = 15.0;